# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Cached download archive (~10 MB) — regenerated by download_data.py.
data/raw/_uci_source.zip

# Keep folder structure but ignore large raw data dumps if needed.
# The project script re-downloads the dataset, so the raw CSV is optional to commit.
# Comment the next line out if you prefer to commit the dataset for offline runs.
# data/raw/*.csv
